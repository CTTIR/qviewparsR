% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_qview.R
\name{read_qview}
\alias{read_qview}
\title{Read a .Q-View project file}
\usage{
read_qview(path, strip_prefix = FALSE, verbose = TRUE)
}
\arguments{
\item{path}{Character. Path to the \code{.Q-View} file.}

\item{strip_prefix}{Logical. If \code{TRUE}, reverse Q-View's internal
naming convention via \code{\link[=strip_qview_prefix]{strip_qview_prefix()}} so identifiers match
the original well-assignment template. Default \code{FALSE}.}

\item{verbose}{Logical. Print a short summary after parsing.
Default \code{TRUE}.}
}
\value{
A list with class \code{"qview"} containing:
\describe{
\item{\code{metadata}}{Named list: \code{project}, \code{plate}, \code{image},
\code{imager}, \code{product}, \code{user}, \code{report_created}, \code{qview_version},
\code{template}, \code{container_version}, \code{file_path}, \code{parsed_at}.}
\item{\code{manifest}}{Tibble with one row per declared file entry
(\code{name}, \code{size_bytes}, \code{parent}).}
\item{\code{segments}}{Tibble of H2 segment byte ranges (\code{segment},
\code{start}, \code{end}, \code{size}).}
\item{\code{analytes}}{Tibble: \code{spot_number}, \code{analyte}, \code{unit}, plus
\code{lod}, \code{lloq}, \code{uloq}, \code{assay_control_low}, \code{assay_control_high}
when reported.}
\item{\code{well_groups}}{Tibble: \code{well_group}, \code{sample_id},
\code{is_standard}, \code{is_negative}, \code{is_sample}, \code{is_control},
\code{well_type}.}
\item{\code{pixel_intensities}}{Long-format tibble of replicate readings:
\code{well_group}, \code{sample_id}, \code{well}, \code{replicate}, \code{analyte},
\code{unit}, \code{pixel_intensity}, \code{dilution}.}
\item{\code{summary_statistics}}{Long-format tibble of per-group
averages, std-dev, and CV statistics: \code{well_group}, \code{sample_id},
\code{statistic}, \code{analyte}, \code{value}, \code{unit}.}
\item{\code{concentrations}}{Long-format concentration tibble or \code{NULL}
if the regression model is \code{"Qualitative"}.}
\item{\code{curve_fit}}{Tibble with \code{analyte}, \code{regression_model}, or
\code{NULL} if not reported.}
\item{\code{report_csv}}{Character vector of the raw CSV report lines,
or \code{NULL} if no report was generated.}
\item{\code{plate_layout}}{Tibble with one row per well: \code{well},
\code{plate_row}, \code{plate_col}, \code{well_group}, \code{sample_id},
\code{well_type}, \code{dilution}.}
}
}
\description{
Parses a \code{.Q-View} binary container (a chemiluminescent multiplex
ELISA project file holding an embedded H2 database plus binary LOB
segments) and extracts the assay data: project metadata, analyte
panel with units, well-group sample assignments, per-well replicate
pixel intensities, summary statistics, and (when present) the
embedded CSV report.
}
\details{
The file format is reverse-engineered from public binary inspection:
it begins with a plain-text manifest, followed by three concatenated
H2 database segments. The fully-formatted report Q-View renders for
the user is stored as a CLOB inside the main H2 segment. This
parser scans the binary for that CLOB, reassembles it across H2 page
boundaries (2048-byte pages), and parses it as CSV.

Parsing is done in pure R: no Java runtime, no H2 database driver,
no system dependencies beyond a working R installation.
}
\examples{
\dontrun{
  qv <- read_qview("plate.Q-View")
  qv
  qv$analytes
  qv$pixel_intensities
  plot(qv, type = "plate_map")
}

}
\seealso{
\code{\link[=strip_qview_prefix]{strip_qview_prefix()}}, \code{\link[=read_qview_template]{read_qview_template()}},
\code{\link[=print.qview]{print.qview()}}, \code{\link[=plot.qview]{plot.qview()}}.
}
