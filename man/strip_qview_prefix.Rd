% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strip-prefix.R
\name{strip_qview_prefix}
\alias{strip_qview_prefix}
\title{Reverse the Q-View internal naming convention}
\usage{
strip_qview_prefix(x)
}
\arguments{
\item{x}{Character vector of Q-View internal names.}
}
\value{
Character vector of the same length as \code{x}, with prefixes
stripped. NAs and unrecognised values pass through unchanged.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
On import, the producing software prefixes well-assignment template names
with single-letter codes:
\itemize{
\item \code{"Cal 1"} ... \code{"Cal N"} -> \code{"ICal 1"} ... \code{"ICal N"} (Internal calibrator)
\item \code{"Low"} -> \code{"GLow"}
\item \code{"High"} -> \code{"HHigh"}
\item \code{"FD..."} and any all-digit ID -> \code{"N..."} (sample prefix)
}

\code{strip_qview_prefix()} reverses this transformation so that downstream
code sees the identifiers exactly as they appear in the original
template CSV. Strings that do not match a known prefix pattern are
returned unchanged.
}
\examples{
strip_qview_prefix(c("ICal 1", "GLow", "HHigh",
                     "NFD24277364", "N1211498458", "Plate 1"))

}
\seealso{
Other qview-helper:
\code{\link[=well_label]{well_label()}}
}
\concept{qview-helper}
