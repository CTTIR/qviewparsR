url: https://cttir.github.io/qviewparsR/

template:
  package: themakR

development:
  mode: auto
includes:
  in_header: |
    <link rel="icon" type="image/svg+xml" href="favicon.svg">


home:
  title: qviewparsR
  description: >
    Pure-R parser for `.Q-View` binary multiplex ELISA project files.

reference:
  - title: Reader
    desc: Parse a Q-View binary project file, or its flat report exports.
    contents:
      - read_qview
      - read_qview_report

  - title: Helpers
    desc: Naming convention and plate-coordinate utilities.
    contents:
      - strip_qview_prefix
      - well_label
      - read_qview_template

  - title: Methods
    desc: Print, summarise, plot, and coerce a parsed `qview` object.
    contents:
      - is_qview
      - print.qview
      - summary.qview
      - plot.qview
      - as_tibble.qview

  - title: Export
    desc: Write a parsed `qview` object to disk.
    contents:
      - write_qview_xlsx
      - write_qview_csv
      - write_qview_rds

  - title: Shiny app
    desc: Upload, preview, visualise, and download a `.Q-View` file.
    contents:
      - qview_app

  - title: Package
    contents:
      - qviewparsR-package

articles:
  - title: Getting started
    navbar: ~
    contents:
      - qviewparsR

# navbar (with themakR's far-right dark/light `lightswitch`), footer, and home
# sidebar are inherited from themakR via `template: package: themakR`. Do NOT
# re-declare `navbar:` here -- overriding its structure drops the `lightswitch`
# component and disables the dark/light toggle.
